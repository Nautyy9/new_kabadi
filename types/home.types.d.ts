export type togglePriceType = {
  togglePrice?: string;
  setTogglePrice?: (val: string) => void;
};
export type toggleTypeType = {
  toggleType?: string;
  setToggleType?: (val: string) => void;
};

export type toggleScrapType = {
  scrapType: string;
  setScrapType: (val: string) => void;
};
